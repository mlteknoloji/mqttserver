const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');const {createSystemSettings}=require('../system-settings');
test('log seçimlerini SQLite veritabanında hatırlar',()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'netrelay-settings-')),databasePath=path.join(root,'settings.sqlite3');let settings=createSystemSettings({databasePath});assert.equal(settings.getBoolean('console_logging_enabled'),true);assert.equal(settings.getBoolean('debug_logging_enabled'),false);settings.setBoolean('console_logging_enabled',false);settings.setBoolean('debug_logging_enabled',true);settings.close();settings=createSystemSettings({databasePath});try{assert.equal(settings.getBoolean('console_logging_enabled'),false);assert.equal(settings.getBoolean('debug_logging_enabled'),true);}finally{settings.close();}});
